use serenity::{model::prelude::GuildId, prelude::Context};

use crate::Handler;

pub async fn handle(handler: &Handler, ctx: Context, guild_id: GuildId, vote_id: i64) {
    let publish_channel_id: i64 = 705438257587945563;
}
